#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    string s;
    cin >> s;
    for(int i =0; i<s.size(); ++i)
        cout << i <<"\n";
    
    return 0;
} // String, Ad_Hoc(???)